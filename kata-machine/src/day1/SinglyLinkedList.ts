export default class SinglyLinkedList<T> {
    public length: number;

    constructor() {
        this.length = 0;
    }

    prepend(item: T): void {
    }

    append(item: T): void {
    }

    insertAt(item: T, idx: number): Node<T> {
    }

    remove(item: T): T | undefined {
    }

    get(idx: number): T | undefined {
    }

    removeAt(idx: number): T | undefined {
    }
}
